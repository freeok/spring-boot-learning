spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mp?serverTimezone=Asia/Shanghai&useUnicode=true&charsetEncoding=utf-8&useSSL=true
    username: root
    password: root
  jackson:
    default-property-inclusion: non_null

mybatis-plus:
  type-aliases-package: com.pcdd.mybatisplus.entity
  configuration:
    # 关闭控制台日志在这里配置
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
